import React, { Fragment } from 'react'
import Header from '../../common/header/Header';

const Details = (props) => {
    return (
        <Fragment>
            <Header/>
            <h2>Details Page</h2>
        </Fragment>
    );
}
export default Details;